--!strict

local types = require(script.Parent.types)

local theme: types.Theme = {
    background = Color3.new(),
    foreground = Color3.new(),
    accent = Color3.new(),
    text = Color3.new()
}
return theme